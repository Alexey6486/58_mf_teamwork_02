# FLIP 7 #
Динамичная карточная игра на риск, память и стратегию.   
Игроки по очереди вытягивают карты из колоды, собирая уникальные номиналы в руке.

## Режим разработки ##
#### Необходимые команды ####

1. `yarn dev` - запуск приложения
2. `yarn bootstrap` - это обязательный шаг, без него ничего работать не будет :) 
3. `yarn dev --scope=client` - запуск только клиента
4. `yarn dev --scope=server` - запуск только server
5. `yarn lint` - линтинг
6. `yarn format` - форматирование prettier
7. `yarn build` - сборка приложения
8. `yarn preview --scope client`, `yarn preview --scope server` - просмотр, что получилось после сборки приложения

#### Docker ####
Перед первым запуском выполните `node init.js`

`docker compose up` - запустит три сервиса
1. nginx, раздающий клиентскую статику (client)
2. node, ваш сервер (server)
3. postgres, вашу базу данных (postgres)

Если вам понадобится только один сервис, просто уточните какой в команде
`docker compose up {sevice_name}`, например `docker compose up server`

**Для работы докера необходимо**
- в корень проекта (на уровне файла docker-compose.yml) добавить файл .env
- данные для этого файла (в учебных целях) лежат в файле .env.sample
- если образы собраны и в проект внесены изменения, то нужно
  - остановить и удалить контейнеры `docker-compose down`
  - пересобрать образы заново `docker-compose build`
  - запустить контейнеры снова `docker-compose up`

**Для работы с БД локально**
- установить PostgreSQL
- запустить pgadmin (устанавливается автоматически при установке PostgreSQL)
- выбрать в панели слева Servers, на дашборде выбрать Add New Server, заполнить:
  - вкладка General:
    - поле name = postgres
  - вкладка Connection:
    - поле Host name/address = localhost
    - port = 5432
    - Maintenance database = postgres
    - username/password = postgres
- запустить проект `yanr dev` (в дев режиме, sequelize настроен на подключение к БД по вышеуказанным данным)

Если клиент на Linux в режиме разработки работает с порта 2000, установите в .env файле
DEV_COOKIE_DOMAIN=localhost:2000

**Работа с запросами к БД с помощью curl**\

Ссылка на postman
`https://www.postman.com/aerospace-specialist-20254567/workspace/flip7/collection/17483266-7833775e-a065-4967-95e7-bd5ccb3a3b4f?action=share&source=copy-link&creator=17483266`

Получить все темы форума:\
`curl "http://localhost:3001/api/v1/forum/topics?page=1&size=20"`

Создать новую тему на форуме:\
`curl -X POST -H "Content-Type: application/json" -d "{\"title\": \"Тема форума №1\", \"text\": \"Текст темы форума №1\", \"authorId\": 1}" http://localhost:3001/api/v1/forum/topic`

Удалить тему на форуме с id=1:\
`curl -X DELETE -H "Content-Type: application/json" -d "{\"id\": 1}" http://localhost:3001/api/v1/forum/topic`

Получить все сообщения темы форума с id=1:\
`curl "http://localhost:3001/api/v1/forum/topic/1/comments"`

Добавить комментарий к теме с id=1:\
`curl -X POST -H "Content-Type: application/json" -d "{\"topicId\": 1, \"text\": \"Комментарий №1 к теме форума №1\", \"authorId\": 1}" http://localhost:3001/api/v1/forum/topic/1/comment`

Добавить реакцию на комментарий с id=1 к теме с id=1:\
`curl -X POST -H "Content-Type: application/json" -d "{\"topicId\": 1, \"text\": \"реакция\", \"authorId\": 1, \"commentId\": 1}" http://localhost:3001/api/v1/forum/topic/1/comment/1/reaction`

___

Более подробная документация для разработчиков находится [по ссылке](docs/oldREADME.md)

## Технологии и структура
### Стэк
**Frontend**: React, TypeScript.   
- Стили: TailwindCSS   
- Состояние: Redux   
- Роутинг: React Router DOM   
  
**Backend**: Node.js, Express   
- База данных: PostgreSQL

**DevOps**: Docker   
**Тестирование**: Jest   
- Линтинг: ESLint, Stylelint, Prettier   
- Git hooks: Husky   

### Страницы приложения
| Ссылка | Страница | Описание |
|----------|----------|----------|
| **`/`** | Страница входа | Авторизация пользователя |
| **`/sign-up`** | Страница регистрации | Создание аккаунта пользователя |
| **`/main`** | Главная страница | Страница с навигацией и с правилами игры. Пользователь попадает на нее после авторизации |
| **`/profile`** | Профиль пользователя | Полная информация об авторизованном пользователе |
| **`/profile/password`** | Страница смены пароля | Пользователь имеет право изменить пароль для входа в систему |
| **`/game`** | Страница игры | Присутствует возможность настроить игру и начать ее |
| **`/leaderboard`** | Таблица лидеров | Рейтинг игроков FLIP 7 |
| **`/forum`** | Страница форума | Игроки могут создавать топики для обсуждения |
| **`/forum/:topicId`** | Страница топика форума | Игроки могут оставлять комментарии под топиками форума |

## Игровая механика
### Цель игры: 
Набрать суммарно 200 и более очков за несколько раундов.   
**При ничьей** (несколько игроков >= 200 очков): побеждает игрок с максимальным количеством очков.

### Карты
| Номинал | Количество |
|---------|------------|
| 1 | 1 |
| 2 | 2 |
| ... | ... |
| 12 | 12 |
| **Итого** | **78 карт** |

### Ход раунда
1. Игроки начинают с одной картой выданной в начале игры
2. Игрок по очереди:   
   ИЛИ остается в игре и берет новую карту из колоды   
   ИЛИ сохраняет набранные очки и выходит из текущего раунда

### Условия завершения раунда
| Действие | Результат | Очки
|---------|------------|------|
| Добровольный выход | Фиксация текущих очков | Сумма руки
| FLIP 7 (7 карт в руке) | Автоматическое завершение + бонус | Сумма руки + 15
| Дубликат карты | Сброс раунда | 0

### Особые правила
1.  Если игрок набрал 7 различных карт, то для него раунд заканчивается, очки записываются и прибавляются бонусные 15 очков.
2.  Если карты в колоде закончились, колода перемешивается и игра продолжается.

### Ссылка на видео с рабочим приложением
https://disk.yandex.ru/i/_txPe6Tsvrg-qA
